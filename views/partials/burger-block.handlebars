<li>
  {{burger_name}}
  {{#if devoured}}
  
  <button class="devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
    <span id="star-img" class="fa fa-star"></span>
  </button>
  {{/if}}
  {{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>